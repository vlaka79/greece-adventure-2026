# inbox

Drop original photos here (HEIC or JPEG with Location on).

SITE reads GPS from these, then puts web-sized copies in `photos/album/`.
This folder is removed at publish, so it never goes live on djbooneadventures.com.

GitHub keeps GPS. Chat does not.
